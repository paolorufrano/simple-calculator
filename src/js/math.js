const add = (a, b) => a + b

const subtract = (a, b) => a - b

const divide = (a, b) => a / b

const multiply = (a, b) => a * b

const percent = value => value / 100

const opposite = number => -number

export { add, subtract, divide, multiply, percent, opposite }
